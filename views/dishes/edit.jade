extends ../layout
block content
  body
  h1= title
  form(method='POST' name='edit-dish' action='/dishes/#{dish._id}?_method=PUT')
    p Dish:
      input(type='text' placeholder='Dish' name='description')
    p Student:
      textarea(placeholder='Who is bringing it?' name='student')
    p
      button(type='submit' class='btn') Update blog
  a(href='/') Back to home

